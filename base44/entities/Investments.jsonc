{
  "name": "Investments",
  "type": "object",
  "properties": {
    "proposal_id": {
      "type": "string",
      "description": "Link to Proposal"
    },
    "jurisdiction": {
      "type": "string",
      "enum": [
        "Local",
        "Offshore"
      ],
      "description": "Investment jurisdiction"
    },
    "currency": {
      "type": "string",
      "enum": [
        "ZAR",
        "USD",
        "GBP",
        "EUR"
      ],
      "description": "Currency"
    },
    "provider": {
      "type": "string",
      "description": "Investment provider"
    },
    "product_type": {
      "type": "string",
      "description": "Product type"
    },
    "underlying_funds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Multi-select underlying funds"
    },
    "fund_allocations": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "Fund rows with fund, allocation, and customFund fields"
    },
    "custom_fund": {
      "type": "string",
      "description": "Custom fund description or additional notes"
    },
    "reason_for_recommendation": {
      "type": "string",
      "description": "Advisor's reason for recommending this product"
    },
    "contribution_type": {
      "type": "string",
      "enum": [
        "Lump Sum",
        "Recurring",
        "Both"
      ],
      "description": "Contribution type"
    },
    "amount": {
      "type": "number",
      "description": "Lump sum amount"
    },
    "recurring_amount": {
      "type": "number",
      "description": "Recurring contribution amount"
    },
    "frequency": {
      "type": "string",
      "enum": [
        "Monthly",
        "Quarterly",
        "Annually"
      ],
      "description": "Frequency if recurring"
    },
    "initial_fee_percent": {
      "type": "number",
      "default": 0,
      "description": "Initial fee percentage (Annexure A / no mandate)"
    },
    "annual_advice_fee_percent": {
      "type": "number",
      "default": 0,
      "description": "Annual advice fee percentage (Annexure A / no mandate)"
    },
    "platform_fee_percent": {
      "type": "number",
      "default": 0,
      "description": "Platform fee percentage (Annexure A / no mandate)"
    },
    "management_fee_percent": {
      "type": "number",
      "default": 0,
      "description": "Annual management fee % of NAV (Annexure B & C)"
    },
    "performance_fee_percent": {
      "type": "number",
      "default": 0,
      "description": "Performance fee % of NAV increase (Annexure B)"
    },
    "hurdle_rate_percent": {
      "type": "number",
      "default": 0,
      "description": "Performance hurdle rate % (Annexure B)"
    },
    "structuring_fee_percent": {
      "type": "number",
      "default": 0,
      "description": "Initial structuring fee % (Annexure C)"
    },
    "raising_fee_percent": {
      "type": "number",
      "default": 0,
      "description": "Annual raising fee % (Annexure C)"
    },
    "carry_fee_percent": {
      "type": "number",
      "default": 0,
      "description": "Performance / carry fee % (Annexure C)"
    },
    "carry_hurdle_percent": {
      "type": "number",
      "default": 0,
      "description": "Carry hurdle rate % (Annexure C)"
    },
    "investment_mandate": {
      "type": "string",
      "enum": [
        "Yes",
        "No"
      ],
      "default": "No",
      "description": "Is this investment subject to a discretionary mandate"
    },
    "applicable_annexure": {
      "type": "string",
      "enum": [
        "A",
        "B",
        "C"
      ],
      "description": "Which annexure this investment belongs to (only if investment_mandate = Yes)"
    }
  },
  "required": [
    "proposal_id"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}